# Production: reverse proxy behind Cloudflare Tunnel (no TLS needed, tunnel handles it)
:80 {
    # API
    handle /api/* {
        reverse_proxy backend:8001
    }

    # Frontend
    handle {
        reverse_proxy frontend:3000
    }
}
